<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="noticeMapper">
  	
  	
  	
  	<insert id="insert" parameterType="NoticeDTO">
  		insert into notice values(board_seq.nextval, #{title}, #{writer}, #{contents}, sysdate)
  	</insert>
  	
  	
  	<select id="totalCount" resultType="int" parameterType="listData">
  			select nvl(count(num),0) from notice
			<if test="kind == 'title'">
				where title LIKE '%'|| #{search}||'%'
			</if>
			<if test="kind == 'writer'">
				where writer LIKE '%'|| #{search}||'%'
			</if>
			<if test="kind == 'contents'">
				where contents LIKE '%'|| #{search}||'%'
			</if>
  		</select>
  	
  	<select id="selectList" parameterType="listData" resultType="NoticeDTO">
  			select * from
  			(select rownum R, N.* from
  			(select * from notice
  			<if test="kind == 'title'">
				where title LIKE '%'|| #{search}||'%'
			</if>
			<if test="kind == 'writer'">
				where writer LIKE '%'|| #{search}||'%'
			</if>
			<if test="kind == 'contents'">
				where contents LIKE '%'|| #{search}||'%'
			</if>			
  			order by num desc) N)
  			where R between #{startRow} and #{lastRow} <!-- Map의 key를 써줌 -->
  		</select>
  		
  		<select id="selectOne" parameterType="int" resultMap="NoticeResult2">
  			select n.*, f.* from notice n, files f 
  			where n.num=#{num} and n.num=f.num(+)
  		</select>
  	
  </mapper>